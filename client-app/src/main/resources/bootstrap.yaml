spring:
  application:
    name: client-app

db-app:
  url: http://db-app:8080

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full